export default interface StyleContext {
  buyerCode: number;
  order: string;
  typeCode: number;
  styleCode: string;
  colorRatio: string;
  sizeRatio: string;
  quantity: number;
  unit: string;
  // ratio: number;
}
